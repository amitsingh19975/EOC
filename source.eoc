```lexer(html)
// import digit;

#0 = "Invalid identifier"
digit = digit | 'a';
digit = digit | 'b';
doc_tag ::= '<', '!', 'DOCTYPE', {whitespace}?, 'html', '>';
start_tag ::= '<', identifier#0, '>'#0;
end_tag ::= '<', '/', identifier, '>';

debug_print
```

```html

<!DOCTYPE html>
<html>
    <body>

        <h1>My First Heading</h1>
        <p>My first paragraph.</p>

    </body>
</html>

```
