keyword defer {
    requires: [.parentBlock]
    (self, $block:block) => {(parentBlock: Block) in
        parentBlock.atExit.push($block)
    }
    
    (self, $expr:expr) => {(parentBlock: Block) in
        parentBlock.atExit.push($expr)
    }
}